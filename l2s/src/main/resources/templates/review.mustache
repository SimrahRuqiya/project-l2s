<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Leave a Review | L2S Rentals</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css">
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <link rel="icon" type="image/png" href="/images/haifa.png">
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            background-color: #fff0f5;
            overflow-x: hidden;
        }
        .navbar {
            position: absolute;
            height: 50px;
            top: 20px;
            left: 0;
            right: 0;
            z-index: 10;
            display: flex;
            justify-content: center;
        }
        .navbar .ui.menu {
            background: rgba(255, 133, 179, 0.5);
            backdrop-filter: blur(8px);
            border-radius: 2em;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        .navbar .item {
            color: white !important;
            font-weight: bold;
            font-size: 1.5rem;
        }
        .ui.segment {
            background: none;
            box-shadow: none;
        }
        .ui.pink.button {
            background-color: #ff85b3 !important;
            color: white !important;
        }
        .review-form-container {
            margin-top: 8em;
            padding: 2em;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 1.5em;
            box-shadow: 0 4px 20px rgba(255, 133, 179, 0.2);
            animation: fadeInUp 1s ease;
        }
        @keyframes fadeInUp {
            0% { opacity: 0; transform: translateY(30px); }
            100% { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>

<div class="navbar">
    <div class="ui secondary menu">
        <a class="item" href="/">Home</a>
        <a class="item" href="/reviews/all">View Reviews</a>
    </div>
</div>

<div class="ui container">
    <div class="ui centered grid">
        <div class="eight wide column">
            <div class="review-form-container">
                <h1 class="ui pink header center aligned">Submit a Review</h1>

                {{#message}}
                    <div class="ui pink message">{{message}}</div>
                {{/message}}

                <form class="ui form" action="/reviews/submit" method="post">
                    <div class="field">
                        <label>Your Name:</label>
                        <input type="text" name="userName" placeholder="Enter your name" required>
                    </div>

                    <div class="field">
                        <label>Select Car:</label>
                        <select name="carId" class="ui dropdown">
                            {{#cars}}
                                <option value="{{carId}}">{{carBrand}} {{carModel}}</option>
                            {{/cars}}
                        </select>
                    </div>

                    <div class="field">
                        <label>Rating (1-5):</label>
                        <input type="number" name="rating" min="1" max="5" placeholder="Rate from 1 to 5" required>
                    </div>

                    <div class="field">
                        <label>Comment:</label>
                        <textarea name="reviewText" rows="4" placeholder="Write your review here..." required></textarea>
                    </div>

                    <button class="ui pink button fluid" type="submit">Submit Review</button>
                </form>

                <div style="margin-top: 2em; text-align: center;">
                    <a href="/home" class="ui pink button">Back to Home</a>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.js"></script>
<script>
    $(document).ready(function() {
        $('.ui.dropdown').dropdown();
    });
</script>
</body>
</html>
