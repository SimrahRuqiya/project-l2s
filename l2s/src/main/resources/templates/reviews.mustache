<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>All Reviews</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css">
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <link rel="icon" type="image/png" href="/images/haifa.png">
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            background-color: #fff0f5;
            overflow-x: hidden;
            padding: 2em;
        }
        .ui.container {
            margin-top: 2em;
            animation: fadeInUp 1s ease;
        }
        .ui.pink.button {
            background-color: #ff85b3 !important;
            color: white !important;
        }
        .review-card {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 1em;
            padding: 2em;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            margin-bottom: 2em;
            animation: fadeInUp 1s ease;
        }
        h1 {
            text-align: center;
            color: #ff85b3;
            margin-bottom: 1.5em;
            font-size: 3em;
            animation: fadeInDown 1s ease;
        }
        .star-rating i.icon {
            color: #ff85b3;
        }
        @keyframes fadeInDown {
            0% { opacity: 0; transform: translateY(-30px); }
            100% { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeInUp {
            0% { opacity: 0; transform: translateY(30px); }
            100% { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>

<div class="ui container">

    <h1>All Reviews</h1>

    {{#reviews}}
        <div class="review-card">
            <p><strong>User:</strong> {{user.userName}}</p>
            <p><strong>Car:</strong> {{car.carBrand}} {{car.carModel}}</p>
            <p><strong>Rating:</strong></p>
            <div class="star-rating">
                {{#starsFilled}}
                    <i class="star icon"></i>
                {{/starsFilled}}
                {{#starsEmpty}}
                    <i class="star outline icon"></i>
                {{/starsEmpty}}
            </div>
            <p><strong>Comment:</strong></p>
            <blockquote>"{{reviewText}}"</blockquote>
        </div>
    {{/reviews}}

    <div class="ui center aligned segment" style="background: none; box-shadow: none;">
        <a href="/home" class="ui pink button">Back to Home</a>
    </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.js"></script>
</body>
</html>
